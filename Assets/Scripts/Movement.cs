using UnityEngine;

public class Movement : MonoBehaviour
{
    public GameObject player;
    void Start()
    {
        
    }

    void Update()
    {
        if (Input.GetAxis("Horizontal") != 0)
        {
            
        }
    }
}
